// ignore_for_file: file_names

import 'package:flutter/material.dart';

class DesignDetails extends StatelessWidget {
  const DesignDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
